# DEMO_VIDEO_FIX_SPEC v1.0 — デモ動画の修正指示書

対象: `figures/demo_video/cosplay_reserve_demo.mp4` を生成しているレンダリングコード
現行版: 180.000s / 1920x1080 / 30fps / **音声トラックなし** / h264 / 8.35MB

この指示書は**動画の見せ方のみ**を対象とする。実験結果・数値・ログには一切触れない。

---

## 0. 前提と禁止事項

### 0.1 まず特定すること

リポジトリ内のファイル名・関数名は不明なので、着手前に以下を特定して報告すること。

1. 動画を生成しているエントリポイント（描画スクリプト or ノートブック）
2. シーン単位の尺（秒数）を決めている定数・config
3. 各シーンのレイアウト座標（タイトル、凡例、キャプション、バー）を決めている箇所
4. `outputs/main_experiment/A_seed2.json` を読んでいる箇所

特定できたら「この5箇所を触る」と宣言してから編集に入る。推測でファイルを新規作成しない。

### 0.2 禁止事項（違反したらロールバック）

- **API を呼ばない。** 追加 run を実行しない。動画は既存 JSON のリプレイのみ。
- **数値を変えない。** `0.45→0.60` / `0.40→0.55` / `20/20 run` / `2/20 run` / `0/20 run` / COMMUNITY SUPPLY のカウント（6→11→14→15→17→21→27）は現行値のまま。見やすくするために丸めたり盛ったりしない。
- **閾値線の位置を動かさない。** バーが「線を越える」絵にするために閾値を左にずらすのは禁止。仕様上、属性は閾値ちょうどまでしか上がらない。**「越える」のではなく「到達する」ことを演出する**（後述 P1-4）。
- **research コード（`src/` 配下のシミュレーション本体、config、preregistration）を編集しない。** 動画側だけを直す。
- レイアウトを直すために要素を削除しない。重なりは「配置を変える」か「レイヤ順を変える」で解決する。

### 0.3 作業ブランチ

`fix/demo-video-layout` を切って作業する。既存の mp4 は `figures/demo_video/cosplay_reserve_demo_v1.mp4` として残す。

---

## 1. 実測データ（再測定不要）

すでに全編フレーム解析済み。以下は測定値なので、そのまま前提にしてよい。

### 1.1 現行のシーン境界（カットは6回のみ）

| # | 区間 | 尺 | 内容 |
|---|---|---|---|
| 1 | 0:00.0 – 0:12.0 | 12.0s | タイトル |
| 2 | 0:12.0 – 0:35.0 | 23.0s | ACCUMULATION グリッド |
| 3 | 0:35.0 – 0:40.0 | 5.0s | ACCUMULATION 個体クローズアップ（agent_5 / IDLE） |
| 4 | 0:40.0 – 1:08.0 | 28.0s | ACCUMULATION グリッド（#2と同一レイアウト） |
| 5 | 1:08.0 – 1:18.0 | 10.0s | SUPPLY SHOCK タイトルカード |
| 6 | 1:18.0 – 2:14.0 | 56.0s | SUPPLY SHOCK 本編（7.0s × 8 step） |
| 7 | 2:14.0 – 3:00.0 | 46.0s | RESULT |

### 1.2 バーのジオメトリ（agent_1 / attr_0、1920x1080 座標系）

- トラック矩形: x = 696 – 816（**幅 121px = 画面幅の6.3%**）、y = 469 – 483（高さ約14px）
- 灰色（既存値 0.45）の右端: 正規化 0.446
- 青（modify 後 0.60）の右端: 正規化 **0.603**
- 閾値線（赤ティック）の中心: 正規化 **0.599**
- **青が伸びる距離 = 19px = 画面幅の 1.0%**
- アニメ: 各 step の開始 +1.0s で出現 → +3.0s で到達 → **+3.0s〜+7.0s は完全静止** → 次 step でリセット
- 8 step すべて同一の動き。差分なし。

### 1.3 完全静止区間（1/5秒精度、変化ピクセル面積 < 0.01%）

| 区間 | 長さ |
|---|---|
| 2:53.8 – 3:00.0 | **6.2s（最終フレームがフリーズしたまま終わる）** |
| 2:31.8 – 2:37.8 | 6.0s |
| 2:48.2 – 2:53.4 | 5.2s |
| 2:38.2 – 2:43.2 | 5.0s |
| 2:43.6 – 2:47.8 | 4.2s |
| 0:08.0 – 0:11.8 | 3.8s |

RESULT 46秒のうち、変化イベントは **10回のみ**（2:14.0 / 2:16.8 / 2:20.0 / 2:23.2 / 2:26.4 / 2:27.8 / 2:31.6 / 2:38.0 / 2:43.4 / 2:48.0 / 2:53.6）。

---

## 2. P0（必須・合計30〜40分）

### P0-1: RESULT 見出しの文字重なり

**現象**: 2:53.6 に表示される白い要約行「3つは全 run で満たされ、届かなかったのは量だけだった。」が、その上のグレーのサブタイトル「事前登録した転化基準（4条件の同時充足）／ 20 run」と**同一の y 座標帯に重なって描画される**。白文字の上端がグレー文字を貫通している。

**なぜ最優先か**: これは動画の**最終フレーム**であり、6.2秒間そのまま静止して終わる。審査員が一時停止して読む確率が最も高いフレームで文字が潰れている。

**修正**:
- 要約行を独立した行として、サブタイトルの**下**に配置する。行間は最低でもフォントサイズの1.4倍を確保。
- または、要約行が出るタイミングでサブタイトルをフェードアウトさせて置き換える（こちらのほうが画面が締まる）。どちらでもよい。
- 検収: 2:54.0 と 2:59.0 のフレームで、2行のバウンディングボックスが**1pxも重ならない**こと。

### P0-2: RESULT の尺を 46s → 28s に圧縮

**現象**: 46秒のうち、4〜6秒の完全静止が5回ある。内容（4条件の内訳／2 of 20 はショック直後1stepのみ／0/20 は share の 0/20 ではない）は良いが、間延びしている。

**修正**:
- 要素の出現間隔を詰める。目安として現行の各インターバルを **0.6倍**にする。
- 4つの条件行（供給者の形成 / 供給の継続 / 協調関係の形成 / 量）は、1行ずつ3.2秒間隔ではなく **1.2秒間隔**で連続的に出す。3つが緑で揃って最後の1つが赤、というリズムが本体なので、間を空けるほど効果が落ちる。
- 黄色の注記ボックス（「量の条件を満たしたのは 2 / 20 run。ただし…」）は表示後 **4秒**保持。
- 最終行「4条件の同時成立（Transition） 0 / 20 run」＋要約行の表示後、**保持は3.0秒**とし、そこで動画を終える。**6.2秒のフリーズフレームは削除する。**
- 目標: RESULT 区間 = **28.0s**。

**削ってはいけない情報**（尺を詰めても全部残す）:
- 4条件それぞれの run 数（20/20 × 3、2/20 × 1）
- 「2/20 はいずれもショック直後の1 step だけ」「share が最大になるのは onset+0、duration >= 4 が成立するのは onset+3。両者の時間帯は重ならない」
- 「『share が 0/20』ではない。0/20 は4条件が同時に成立した run の数」
- 下部の source 表記（`transition_recomputed_preregistered.json` / `preregistered D4 / corrected adjudication`）

### P0-3: タイトル画面の重なり

**現象**: 0:00–0:11 で、背景のエージェントアイコン格子の1行目が `COSPLAY RESERVE` の上端に接触し、**2行目のアイコンが日本語サブタイトル「文化活動が蓄積した制作能力を、供給ショックで測る」を貫通している**。

**修正**（いずれか1つで足りる。上から順に推奨）:
1. 背景格子のアイコンとエッジの不透明度を **0.18** 程度まで下げ、文字ブロックの背後に敷く。文字は最前面レイヤに。
2. 文字ブロックを格子の下（y ≈ 620 付近）に移し、格子と縦に分離する。
3. 文字ブロックの背後に、背景色と同色の矩形（角丸・不透明度0.85）を敷いて抜く。

**同時に直すこと**: 現行のタイトル画面は要素が左に寄り、**画面右3割（x > 1290）が完全な空白**。格子と文字ブロックをまとめて水平中央に寄せる。

**検収**: 0:02 / 0:06 / 0:10 のフレームで、タイトル文字・サブタイトル文字の外接矩形と、アイコンの外接矩形が交差しないこと。

---

## 3. P1（受賞狙いなら実施・合計60〜80分）

### P1-4: 閾値到達を見えるようにする

**現象**: 「未知仕様への適応が起きた」という本作の中核イベントの視覚表現が、**画面幅の1.0%（19px）を2秒かけて動く青い矩形**でしかない。到達時の色変化・発光・線の反応が一切ない。しかも8 step まったく同一の動きを反復するため、視聴者は3回目で見るのをやめる。

**修正（4点セットで実施。1つだけでは効かない）**:

**(a) バーを大きくする**
- トラック幅 121px → **240px**、高さ 14px → **22px**。
- 6体を3列×2行のまま維持できないなら、ショック相のレイアウトを **上段3体を大きく／下段3体を小さく** の非対称にしてよい。全員同じ大きさで見せる必要はない。

**(b) 到達の瞬間に状態変化を出す**
- 青の右端が閾値線に到達したフレームで:
  - 閾値線を **1フレームだけ白（#FFFFFF）にフラッシュ**させ、以降は緑（`SUPPLY +1` と同じ緑）に変える
  - バーの塗りを青 → 緑にトランジション（0.2秒）
  - `attr_0` `attr_2` **両方**が緑になった時点で、そのエージェントのアイコンの輪郭を0.3秒だけ太くする
- **重要**: これは表示上の状態変化であって、数値は変えない。到達判定は既存のログ値（0.60 / 0.55）から導出すること。閾値を下げて「越えた」ように見せるのは禁止。

**(c) ラベルを「越えた/越えない」ではなく「届いた」にする**
- 現行の `0.45→0.60` の右に、到達時点で `= 0.60 ✓ required` を緑で追記。
- 仕様上ちょうど閾値で止まるのが正しい挙動なので、**「ぴったり届いた」ことを見せる**のが正解。オーバーシュートする絵を作ってはいけない。

**(d) 8 step の反復を1回だけ断ち切る**
- step 3 または step 4 の到達タイミングで、**1体（agent_1 推奨）のバー2本に 3.5秒だけ寄る挿入カット**を入れる。
- そのカットでは、バーを画面幅の50%以上に拡大し、閾値線と到達を大きく見せる。
- 挿入カット中に小さくキャプションを出す: `2箇所の modify で仕様に到達（成功確率 0.980 → 0.576）`
  - この数値は既存の RESULTS.md / 感度分析の値。新規計算はしない。値が確認できなければキャプションは省略してよい（**数値を推測で書かない**）。
- 挿入カット明けは元のグリッドに戻す。

### P1-5: 蓄積相の圧縮とクローズアップ差し替え

**現象**:
- 0:40–1:08 の **28秒が完全に同一レイアウト**。カットが2回しかない。
- 蓄積を示すのは右カラムの小さな数字（completed projects 2,539 / MAKER STAGE 20-10 / mean 0.43）だけで、「溜まっていく」感覚が絵になっていない。
- **0:35 の唯一の個体クローズアップが agent_5 の `IDLE`**。制作の話の唯一のヒーローショットが「何もしていない」状態。

**修正**:

**(a) クローズアップの対象を差し替える**
- `outputs/` の M1 trace（condition A / seed 2）から、**`make ... -> completed` を含む週**を選んで表示する。
- 優先順位: ① `ask` → 次週に `make` 成功、が並んでいる agent/week（peer learning が効いた瞬間になる） ② `make -> completed` を含む週 ③ それも無ければ `practice` が連続する週
- **`idle` がハイライト行（`▶`）になる週は選ばない。**
- 選定は決定論的に行い、選んだ agent_id と week をコード内のコメントかログに残す（cherry-pick ではなく選定規則であることが分かるように）。

**(b) 尺を 56s → 42s に**
- グリッド区間（現 23s + 28s = 51s）を **34s**に。週送りの速度を上げる（現行 x30 → x45 程度）。
- クローズアップは 5s → **8s**に**延ばす**。ここが唯一「1体が何をしているか」が読める箇所なので、短くするのは逆。

**(c) 蓄積が「積み上がる」絵を1つ足す**
- 右カラムの `completed projects` の数字の下に、**週ごとの累積本数を描く細い折れ線 or エリアチャート**を1つ追加する（週が進むと右に伸びる）。
- 既存 trace の集計のみで描けるはずなので、新規シミュレーションはしない。
- 描けない/時間がなければスキップしてよい。P0 を優先。

### P1-6: 凡例の重なり

**現象**: 0:12–1:08 の左下、キャプション `ACCUMULATION` / `source = M1 trace / condition A / seed 2` が、その上の凡例の `PRACTICE` アイコンとラベルに重なっている。

**修正**: キャプションブロックを下に約 40px 移動するか、凡例行を上に約 40px 移動する。どちらでもよい。
**検収**: 0:45 のフレームで `PRACTICE` の文字と `ACCUMULATION` の文字が重ならないこと。

---

## 4. P2（時間が余れば）

### P2-7: 3分間の無音

現行の mp4 には**音声ストリームが存在しない**。ナレーション無しの方針は維持してよいが、完全な無音は再生環境によっては「ファイルが壊れている」と受け取られるリスクがある。

- 小音量の環境音か、step 進行のクリック音（-24 LUFS 程度）を1トラック入れる。
- 音楽を入れる場合は**ライセンス表記をリポジトリの README に追記**すること。ライセンスが確認できない素材は使わない。
- 迷ったら無音のまま提出してよい。P0 / P1 を優先。

---

## 5. 修正後の目標タイムライン

| # | 区間 | 現行 | 目標 | 差分 |
|---|---|---|---|---|
| 1 | タイトル | 12.0s | 8.0s | -4.0 |
| 2 | ACCUMULATION グリッド前半 | 23.0s | 17.0s | -6.0 |
| 3 | ACCUMULATION クローズアップ | 5.0s | 8.0s | **+3.0** |
| 4 | ACCUMULATION グリッド後半 | 28.0s | 17.0s | -11.0 |
| 5 | SUPPLY SHOCK カード | 10.0s | 6.0s | -4.0 |
| 6 | SUPPLY SHOCK 本編 | 56.0s | 56.0s | 0（うち3.5sを挿入カットに置換） |
| 7 | RESULT | 46.0s | 28.0s | -18.0 |
| — | **合計** | **180.0s** | **140.0s** | **-40.0** |

140s = 2:20。提出要件は「約3分」なので、**余った40秒はショック相に戻す**のが望ましい:

- step あたり 7.0s → 9.0s（8 step で 72.0s、+16.0s）にして、到達アニメの後の静止4秒を「他エージェントの `MAKE FAILED` を順に見せる」時間に充てる
- 末尾に6秒のクロージングカード（タイトル＋GitHub URL＋一行の主張）を追加
- これで **合計 = 162s（2:42）**。要件内。

**尺の再配分は上記どおりでなくてよいが、以下は守ること:**
- 総尺 **150s〜180s** に収める
- RESULT は **30s を超えない**
- 4秒を超える完全静止区間を作らない（末尾のクロージングカードのみ例外、最大4秒）

---

## 6. 検証手順

修正後の mp4 に対して以下を実行し、結果を報告すること。

### 6.1 尺と音声

```bash
ffprobe -v error -show_entries format=duration -show_entries stream=codec_type \
  -of default=noprint_wrappers=1 figures/demo_video/cosplay_reserve_demo.mp4
```

期待: `duration` が 150–180 の範囲。音声を入れた場合は `codec_type=audio` が1つ出ること。

### 6.2 完全静止区間の検出

```bash
ffmpeg -v error -i figures/demo_video/cosplay_reserve_demo.mp4 \
  -vf "fps=5,scale=960:540" -pix_fmt gray -f rawvideo - 2>/dev/null | python3 -c "
import sys,numpy as np
d=sys.stdin.buffer.read(); n=960*540
fr=np.frombuffer(d,dtype=np.uint8)[:len(d)//n*n].reshape(-1,540,960).astype(np.int16)
area=(np.abs(np.diff(fr,axis=0))>12).mean(axis=(1,2))*100
runs=[];st=None
for i,a in enumerate(area):
    t=(i+1)/5
    if a<0.01:
        if st is None: st=t
    else:
        if st is not None and t-st>=2: runs.append((st,round(t-0.2,1),round(t-0.2-st,1)))
        st=None
if st is not None: runs.append((st,len(area)/5,round(len(area)/5-st,1)))
print('cuts:',[round((i+1)/5,1) for i,a in enumerate(area) if a>3])
print('static runs >=2s:')
for r in sorted(runs,key=lambda x:-x[2]): print(f'  {r[0]:.1f}s - {r[1]:.1f}s  ({r[2]:.1f}s)')
"
```

期待: **4.0s を超える静止区間が末尾のクロージングカード以外に存在しないこと。** カット数が現行の6回から**10回以上**に増えていること。

### 6.3 文字重なり

以下のフレームを書き出して目視確認し、判定を報告する。

```bash
for t in 2 6 45 <RESULT末尾の秒数>; do
  ffmpeg -v error -ss $t -i figures/demo_video/cosplay_reserve_demo.mp4 \
    -frames:v 1 /tmp/check_$t.png -y
done
```

チェック項目:
- [ ] 0:02 / 0:06 — アイコンがタイトル文字・サブタイトル文字に重なっていない
- [ ] 0:45 — `ACCUMULATION` キャプションが `PRACTICE` 凡例に重なっていない
- [ ] RESULT 末尾 — サブタイトルと要約行が重なっていない

### 6.4 数値の不変性

修正前後で以下が**一字一句同じ**であることを確認する（フレームを目視で照合）:

- `attr_0 >= 0.60` / `attr_2 >= 0.55`
- `0.45→0.60` / `0.40→0.55`
- COMMUNITY SUPPLY の推移: 6 → 11 → 14 → 15 → 17 → 21 → 27
  - **【訂正 2026-08-21】この参照リストは step 2 の `8` が脱落している。**
    実データは 8 step 分あり **`6 → 8 → 11 → 14 → 15 → 17 → 21 → 27`（8値）** が正。
    ログを正とし、動画の表示は変更しない（計算式は修正前後で不変）。
- `active_supplier_count >= 3` → `20 / 20 run`
- `supply_duration >= 4 step` → `20 / 20 run`
- `coordination_edges >= 2` → `20 / 20 run`
- `community_supply_share >= 0.25` → `2 / 20 run`
- `4条件の同時成立（Transition）` → `0 / 20 run`

**1つでも変わっていたらバグ。ロールバックして原因を報告すること。**

---

## 7. 完了条件

- [ ] P0-1 / P0-2 / P0-3 完了
- [ ] 6.2 の静止区間チェックを通過
- [ ] 6.3 の重なりチェック3項目すべて通過
- [ ] 6.4 の数値不変性チェック通過
- [ ] 旧版が `cosplay_reserve_demo_v1.mp4` として残っている
- [ ] 触ったファイルの一覧と、各ファイルで何を変えたかを1行ずつ報告
- [ ] P1 のうち実施したもの／見送ったものを明記（見送りは可。**見送った事実を書かないのは不可**）

---

## 8. 時間が足りない場合の縮退順序

上から順に切り捨ててよい。

1. P2-7（音声）
2. P1-5(c)（累積チャートの追加）
3. P1-4(d)（挿入カット）
4. P1-5(b)（蓄積相の尺調整）
5. P1-6（凡例の重なり）
6. P1-5(a)（クローズアップの差し替え）
7. P1-4(a)(b)(c)（バーの視認性）

**P0-1 / P0-2 / P0-3 は縮退不可。** この3つだけでも実施すること。
