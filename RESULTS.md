# RESULTS — Phase 1 / Milestone 1–3

**確定日**: 2026-08-16（人間判断により研究実験フェーズ終了）
**正典**: 研究命題・理論・実験原則は `SPEC.md`。転化閾値は `docs/PREREGISTRATION_H1.md` §D4。
**限界**: `docs/LIMITATIONS_CANDIDATES.md` L1–L16。

> **本ドキュメントは「研究上の結果」と「製品仮説」を明確に分離する。**
> §1–§12 が研究結果、§13–§14 が製品への接続である。**§13 以降は研究結果からの直接の含意ではなく、
> 商用化前に現実で測るべき対象を特定したものである。**

---

## 1. Executive Summary

未知の需要仕様に対して、モデル内のエージェント群は**答えを与えられないまま**既存の制作対象を
要求仕様まで作り替え、供給に到達した。供給者の形成・供給の継続・協調関係の形成は
**20 run すべてで観測された**。

しかし、事前登録した転化基準（4条件の同時充足）を満たした run は **0 / 20** だった。
唯一の律速は**量**（`community_supply_share >= 0.25`）である。

ボトルネックを分解した結果、量を制約していたのは**材料でも設備でも時間でもなく**、
**要求仕様へ作り替えたあとの制作成功確率**だった。仕様適応のための modify が課す難度ペナルティ
`modify_difficulty_penalty` が成功確率を 0.98 → 0.58 へ下げていた。

このパラメータは**実データで校正されていない**。感度分析の結果、供給量はこの単一パラメータで
**50% 変動し**、転化確率は 0.00〜0.15 の狭い区間で **0.988 → 0.066 へ崩落**した。
実験の主要因子（topology × peer learning）による差の**約 7.4 倍**である。

量的条件に届くかどうかの**D4 供給閾値相当の感度境界は P\* = 0.0426**（§8.5）であり、
current model の 0.35 は**その約 8.2 倍**にあたる。**現実の値がこの境界のどちら側にあるかは未知である。**

したがって本研究の最終的な成果は、供給能力の推定値ではなく、
**「商用化の前に現実で何を測らなければならないか」の特定**である。
その最重要項目は **Specification Adaptation Success**（実際の制作者が未知仕様へ適応するときの
成功率・制作時間・歩留まりの変化）であり、次の実地 Pilot の測定対象である。

---

## 2. Research Question

`SPEC.md` §2 / §7 より。

> **中心仮説**: 文化は「何を望むか」（Soft Power）だけでなく「何をできるか」（Latent Capability）
> まで変える。経路は `Cultural Participation → Latent Capability` である。

検証対象の仮説：

| | 内容 |
|---|---|
| **H1** | 相互学習構造は時間経過とともに Maker 人口を増加させるか |
| **H2** | 初期の技能・設備が同一でも、Capability Reproduction Loop があると Latent Capability がより速く成長するか |
| **H3** | その蓄積差は供給ショック時の転化確率・転化速度・供給量に差を生むか |

**未証明の主張はモデルの前提に置いていない**（SPEC §6）。コスプレイヤーが他人より利他的である、
コスプレコミュニティが他より強い、といった命題は検証対象であって仮定ではない。

---

## 3. Experimental Design

### 3.1 2×2 完全要因計画（SPEC §19）

因子は **topology**（structured / rewired）× **peer learning**（ON / OFF）。

| | peer learning ON | peer learning OFF |
|---|---|---|
| **structured** | **A** | **C** |
| **rewired** | **B** | **D** |

- 同一 seed において **A と C は同一の Graph object 由来**、**B と D も同一の rewired graph 由来**
  （base graph を1回生成し deep copy で配布。`peer_learning_enabled` フラグのみ変更）
- 初期技能・設備・資源は4条件で**完全に同一**（pre-network 初期状態ハッシュが4条件一致）
- 条件別 YAML の差分は `topology` と `peer_learning_enabled` の **2キーのみ**
- **C/D は「ネットワークを除去した世界」ではない。** 無効化されるのは他 Agent からの Method 取得と
  peer 由来 Method による social scaffolding のみ

### 3.2 二相クロック（SPEC §25）

| 相 | 1 step | step 数 |
|---|---|---|
| 蓄積相 | 1 週 | 156 |
| ショック相 | 6 時間 | 8 |

### 3.3 母集団と Milestone

N=40（participant 30 / non-participant 10）。**participant / non-participant は主要な因果対照ではない**
（`docs/DESIGN_M1.md` §3.4）。主要対照は A/B/C/D の 2×2 である。

| Milestone | 内容 |
|---|---|
| M1 | LLM なしの機構（Agent 生成 / network / observe・practice・make・share / 段階遷移） |
| M2 | event-driven な局所 LLM 意思決定（最小構成、freeze 済み） |
| M3 | 供給ショック、コード側の転化判定 |

### 3.4 answer leak の遮断

- **Action に「答え」をハードコードしない。** `make_ppe` のような Action は存在せず、
  `make` / `modify` / `share` / `propose` / `join` 等の一般化 Action のみ
- 需要は名称ではなく**属性ベクトル `attr_0`〜`attr_6`** で表現。判定は属性閾値のみで、**名称照合をしない**
- Agent に渡る文字列は中立コード表記（`skill_N` / `mat_N` / `asset_N` / `proj_N` / `attr_N`）
- 禁止語テストを Agent-facing strings に対して実施（語境界マッチ、陽性対照12件・陰性対照6件）

### 3.5 使用 seed（構造的 eligibility に基づく事前選定）

**固定 seed: 2, 4, 6, 7, 9**（scan 範囲 1〜9、ineligible 1, 3, 5, 8）。

判定規則は「structured(A,C) と rewired(B,D) の双方で `structurally_available_pairs >= 2`」。
**LLM の Intent / 供給量 / 転化結果など Agent の行動結果を一切参照していない。**
これは outcome selection ではなく、測定可能性に基づく pre-experiment eligibility check である。

> **一般化範囲の制約**: 推論対象は「`coordination_edges >= 2` が structured / rewired の双方で
> 構造的に測定可能なネットワーク realization」に**限定される**。
> **9 seed 中 4 seed（44%）が ineligible** だった。**conditional sample である。**

### 3.6 実験規模

| 項目 | 値 |
|---|---|
| run 数 | 20（4条件 × 5 seed） |
| shock_agent_count | 6 |
| shock_steps | 8 |
| LLM 呼び出し | 960（48/run） |
| model / prompt | `claude-opus-5` / `m2-minimal-v1`（20 run で1種類） |
| 実支出 | $18.503645 |
| 実行順序 | 事前ランダム化（`order_seed=20260816`） |

---

## 4. Main Experiment Result

### 4.1 実行状況

20 / 20 完了、失敗 0。attempt は 19 run が1回、1 run（B seed6）が2回。予算超過なし。
`prompt_sha256` と `code_git_commit` は 20 run で1種類、`config_sha256` の差は `run.seed` のみ。

**ペアリング健全性**: `structurally_available_pairs` は全 seed で **A=C、B=D** が一致
（seed2: 3/3・3/3、seed4: 5/5・2/2、seed6: 2/2・2/2、seed7: 3/3・2/2、seed9: 3/3・2/2）。
`shock_agent_ids` も4条件で同一。

### 4.2 主結果 — Transition = 0 / 20

**正式事前登録 D4（`share>=0.25` / `suppliers>=3` / `duration>=4` / `edges>=2`）による判定：**

| 条件 | Transition TRUE | FALSE |
|---|---|---|
| A | **0 / 5** | 5 / 5 |
| B | **0 / 5** | 5 / 5 |
| C | **0 / 5** | 5 / 5 |
| D | **0 / 5** | 5 / 5 |
| **計** | **0 / 20** | 20 / 20 |

**これは失敗ではない。** 4条件のうち3条件は全 run で充足しており（§6）、
未充足は量の1条件のみである。**一点物メイカーの能力プロファイルの測定結果**として読む。

### 4.3 ボトルネックは量の1条件のみ

全 step を通じて一度も充足できなかった条件：

| 条件 | 未充足 run 数 |
|---|---|
| **`community_supply_share`（>=0.25）** | **18 / 20** |
| `active_supplier_count`（>=3） | 0 / 20 |
| `supply_duration`（>=4） | 0 / 20 |
| `coordination_edges`（>=2） | 0 / 20 |

share が 0.25 に到達したのは A seed2 / A seed7 の t+0 のみ（いずれもちょうど 0.25、
`6.0/24.0` で厳密表現可能。丸め由来の反転ではない）。ただしその時点で `duration=1 < 4` であり、
**share が最も高い時間帯と duration が資格を得る時間帯（t+3 以降）が重ならない。**

### 4.4 2×2 比較（強い統計的主張はしない）

`community_supply_share` の条件平均：**A=0.1731 / B=0.1796 / C=0.1710 / D=0.1701**

| 効果 | 値 |
|---|---|
| topology 主効果（A,C − B,D） | −0.0028 |
| peer learning 主効果（A,B − C,D） | +0.0058 |
| 交互作用（A−B）−（C−D） | −0.0074 |

いずれも seed 内変動（−0.038 〜 +0.047）より**1桁小さく、seed 間ばらつきに埋もれている**。

`modify_count` では peer learning ON が一貫して多い（主効果 +1.9、A−C と B−D がともに 5 seed 中 4 seed で正）
一方、`coordination_edges` は **peer learning OFF のほうが多い**（主効果 −0.4）。方向は仮説と逆である。

> **n=5/セルは結論を出せる規模ではない。** H1/H2/H3 について本実験から結論を主張しない。
> M1 の holdout では H1 は事前登録した基準で**支持されなかった**（`mean_time_to_maker`、B−D 13/20 < 15/20）。

---

## 5. Corrected Adjudication and D4 Deviation

### 5.1 何が起きたか

main experiment 20 run は、正式事前登録 D4 ではなく **PIPELINE_VALIDATION 時の暫定値**
（`share>=0.20` / `duration>=3`）で実行された。

| 閾値 | 実行時 | 正式事前登録 | 一致 |
|---|---|---|---|
| `community_supply_share` | 0.20 | **0.25** | ✗ |
| `active_supplier_count` | 3 | **3**（ceil(6/2)） | ✓ |
| `supply_duration_steps` | 3 | **4**（24h） | ✗ |
| `coordination_edges` | 2 | **2** | ✓ |

**分類: config synchronization error（主因）+ frozen-spec test error（検出漏れ）。**

D4 を確定した commit `2de6b52` が `configs/base.yaml` の **D5 ブロックだけを書き換え、
直下の D4 ブロックを素通り**した（同 commit の diff に旧 D4 コメントが context 行として残存）。
確定値は `PREREGISTRATION_H1.md` と `m3_main.py` の docstring にのみ記録され、
**runtime が読む config には到達しなかった。**

**なぜ検出されなかったか**: 実行後の逸脱チェックが「20 run で同じ値だったか」という
**内部整合性**しか検査せず、「事前登録値と一致しているか」という**外部基準との照合**を欠いていた。

### 5.2 corrected adjudication

既存ログを使い、事前登録値へ戻して再判定した（**API 0 call**）。

- **判定ロジックを再実装していない。** 既存の `TransitionJudge.evaluate()` に、
  ログ済みの閾値非依存な実測値（share / suppliers / duration / edges）を再投入した
- **自己検証**: runtime 閾値で replay すると 20 run × 8 step 全件で `met_*` / `all_met` /
  `transition_step` がログと**完全一致**

| 判定 | Transition |
|---|---|
| **主結果** — 事前登録値（0.25/3/4/2） | **0 / 20** |
| 副次的記録 — 実行時暫定値（0.20/3/3/2） | 6 / 20 |

**6 run（A7, A9, B9, C4, C9, D6）がすべて TRUE → FALSE へ変化。逆方向はゼロ。**

> **主結果は corrected adjudication の 0/20 である（人間確定）。**
> **runtime の 6/20 は主結果に使用しない。** 事前登録との不一致を示す副次的記録として
> 各 run ファイルに保持している（改変していない）。

### 5.3 是正

1. 実行時 config を `configs/as_executed/main_experiment_20260816.yaml` へ歴史的記録として保存
   （実行 commit の `base.yaml` とバイト単位で同一。編集禁止）
2. `configs/base.yaml` の D4 を正式事前登録値へ同期。**第三者が現在の config から実行すれば
   正式事前登録値による判定になる**
3. 事前登録 / config / run metadata の**三者突合**監査を実装（`experiments/audit_preregistration.py`）
4. 同期テストを常時 PASS 化（`tests/test_preregistration_sync.py`）

**既存 20 run の metadata と output は一切変更していない。**

---

## 6. Mechanism Findings

**正式事前登録 D4 の下で、4条件のうち3条件は全 run で充足された。**

| 問い | 判定基準 | 結果 |
|---|---|---|
| 仕様を満たす供給者が形成されたか | `active_supplier_count >= 3` | ✅ **20 / 20 run** |
| 供給が継続したか | `supply_duration >= 4 step (24h)` | ✅ **20 / 20 run** |
| 協調関係が形成されたか | `coordination_edges >= 2` | ✅ **20 / 20 run** |
| 量が閾値に達したか | `community_supply_share >= 0.25` | ❌ 転化 **0 / 20 run** |

### 6.1 仕様適応 — provenance に直接記録された事実

Agent には答えを一切与えていない。それでも Agent は**既存の制作対象を要求仕様まで modify して
供給に到達した**。これは集計値からの推論ではなく、**make 試行ごとに記録された事実**である。

```
before  attr_0=0.45, attr_2=0.40   （要求を満たさない既存の制作対象）
modify  attr_0 +0.15, attr_2 +0.15 （Agent 自身の判断）
after   attr_0=0.60, attr_2=0.55   → 要求仕様 (>=0.60, >=0.55) を充足 → 供給成立
```

3条件（A seed7 / C seed9 / B seed9）で同一構造の経路を確認。判定は属性閾値のみで、**名称照合をしていない**。

### 6.2 協調

供給を伴う make 記録 606 件のうち、**282 件が同時刻に協調関係を持つ作り手によるもの**（20/20 run で発生）。
ただし**初回供給の時点では協調 edge は 0** であり、協調は供給の必要条件ではない。両者は独立に成立している。

### 6.3 観測されなかったもの

- **解の多様性は低い。** 供給に使われた制作対象は 2 種のみ（606 件中 579 件が同一）。
  modify の内容も「2属性を各 +0.15」がほぼ全 run で反復
- **Emergence level は transition の言い換えになっている。** E0/E1/E3 は 1 run も出ず、
  E2（未転化）と E4（転化）の2値のみ。暫定定義の識別力が不足している

---

## 7. Bottleneck Decomposition

条件A の 5 run について、供給量を制約していた要因を分解した（**既存ログのみ、API 0 call**）。

### 7.1 資源による阻害はほぼ存在しない

| 要因 | rejection 件数（intent 提案 921 件中） |
|---|---|
| 材料不足 | 9（0.98%） |
| 設備なし | 1（0.11%） |
| 時間超過 | 5（0.54%） |
| **計** | **15（1.6%）** |

**make 試行 280 件のうち、材料不足で供給に至らなかったものは 0 件、設備なしも 0 件**
（`material_feasible` / `asset_feasible` が偽の記録なし）。

### 7.2 時間は余っていた

| 指標 | 値 |
|---|---|
| 総時間予算の使用率 | **65.0%**（未使用 35.0%） |
| make に使った時間 | 38.9% |
| modify に使った時間 | 9.1% |
| 行動数 | 178〜183 / 上限 288 |
| idle の選択回数 | 54 |

**時間が余っている状態で idle が選択されている。** 時間を増やしても使い道が生まれない。

### 7.3 律速は make の成功確率

make 試行 280 件が供給に至らなかった理由：

| 要因 | 件数 | 比率 |
|---|---|---|
| 供給成立 | 151 | 53.9% |
| **確率的な制作失敗** | **112** | **40.0%** |
| 仕様未達 | 17 | 6.1% |
| 材料不足 | **0** | 0.0% |
| 設備なし | **0** | 0.0% |

**成功確率を下げていたのは、仕様適応のための modify そのものである：**

| 適用した modify 箇所数 | 件数 | 平均成功確率 | 実測成功率 |
|---|---|---|---|
| 0箇所 | 10 | **0.980** | 100.0% |
| 1箇所 | 4 | 0.726 | 25.0% |
| **2箇所** | **266** | **0.576** | 57.1% |

供給経路の 95% が 2箇所 modify であり、`modify_difficulty_penalty = 0.35` により
成功確率が **0.98 → 0.58** へ落ちていた。

> **現行モデルでは、仕様適応と量産が直接トレードオフになっている。**

### 7.4 capacity gap

D4 を `duration=4` の時点で満たすには `C/(C+4×18) >= 0.25` すなわち **平均 6.0 units/step** が必要。

| seed | 実測 units/step | 6.0 との差 | 必要倍率 |
|---|---|---|---|
| 2 | 3.50 | −2.50 | 1.71× |
| 4 | 4.00 | −2.00 | 1.50× |
| 6 | 3.50 | −2.50 | 1.71× |
| 7 | 3.75 | −2.25 | 1.60× |
| 9 | 4.75 | −1.25 | 1.26× |

決定的な数字：

| 段階 | units/step |
|---|---|
| make 試行 | 7.00 |
| **仕様適合 make**（成功確率 1.0 なら供給されうる上限） | **6.58** |
| 実供給 | 3.77 |
| **必要（D4）** | **6.00** |

**仕様適合まで到達した試行だけで必要量を上回っている。** 失われているのは成功確率の部分である。

### 7.5 介入実験を実施しなかった理由

当初検討した3介入は、いずれも**観測されたボトルネックに対応していなかった**：

| 介入 | 判定 | 根拠 |
|---|---|---|
| I1 材料備蓄 | **NOT SUPPORTED** | 材料由来の供給阻害が make 試行 280 件中 0 件。全 9 件の rejection が成功しても gap の約 6% |
| I2 共用設備 | **NOT SUPPORTED** | 設備由来の阻害が 0 件。rejection は 5 run で 1 件 |
| I3 時間の解放 | **NOT SUPPORTED** | 時間予算の 35% が未使用、idle が 54 回選択されている |

**「現実に売りやすい介入だから」という理由で介入実験を実施しなかった。**
実施しないこと自体を診断結果として記録する。

> ⚠️ **これは「現実でも材料備蓄・共用設備・時間確保が無効である」ことを意味しない。**
> 本モデルは材料・設備を agent ごとの私有資源として実装しており、共有プールの競合も、
> 現実の調達リードタイムも、設備の稼働待ちも表現していない。
> **モデル内で律速でなかったことと、現実で無効であることは別である。**

---

## 8. Penalty Sensitivity Analysis

`modify_difficulty_penalty = 0.35` は**実データで校正されていない**。§7 の診断がこの単一パラメータに
どの程度依存するかを測った（**事前登録済み**、`docs/PREREGISTRATION_SENSITIVITY.md`）。

### 8.1 方式 — deterministic replay（API 0 call）

既存 provenance から `p_base` を復元し、production layer だけを再計算した。

```
p_base = p_eff_logged × (1 + 0.35 × n_shifts)
```

`success_probability()` は penalty を参照しないため、この復元は**厳密**である（全 1110 試行で復元可能）。

**唯一の帰還経路（成功/失敗 → 技能 → p_base）が不活性であることを証明した**：
技能は 0.9976〜0.9985 に飽和し `p_base` の 99.6% が上限 0.98 にクリップされている。
最悪ケースの技能乖離 0.0008 は、クリップまでの余裕 0.314 の約 1/400 である。

**手法の妥当性検証**: `penalty=0.35` の解析期待値 612.79 が実測 606.0 を **−1.1%** で再現
（二項ノイズの範囲、系統誤差なし）。

### 8.2 結果

| penalty | units/step | 6.0 との差 | make success rate | share = C/(C+B) | parity utilization = C/B |
|---|---|---|---|---|---|
| **0.00** | **6.511** | **+0.511** | 0.9800 | 0.2656 | 0.3617 |
| 0.15 | 5.008 | −0.992 | 0.7611 | 0.2177 | 0.2782 |
| **0.35** (current) | **3.830** | **−2.170** | 0.5894 | **0.1754** | **0.2128** |
| 0.50 | 3.255 | −2.745 | 0.5056 | 0.1532 | 0.1809 |

> **current model の期待供給率は External Supply Parity Reference の約 21% に相当し、
> D4 の share 閾値 0.25 を満たすには約 33% 相当が必要だった。**
>
> ここで「約 21%」は **parity utilization ratio（= C/B）** であり、
> `community_supply_share`（= C/(C+B) = 0.175）とは**別量**である（L15）。
> D5 は現実の外部メーカー能力の実証値ではなく正規化基準であるため、
> この比率を現実の設備稼働率と読んではならない。

**転化確率（CRN Monte Carlo R=2000、正式 D4）：**

| penalty | 転化確率 | 転化確率≥50% の run |
|---|---|---|
| 0.00 | **0.9878** | 20 / 20 |
| 0.15 | 0.0662 | 0 / 20 |
| 0.35 | 0.0003 | 0 / 20 |
| 0.50 | 0.0000 | 0 / 20 |

### 8.3 感度の大きさ

| 指標 | 値 |
|---|---|
| 供給率 0.00 → 0.50 | 6.511 → 3.255（**−50.0%**） |
| penalty による変動幅 | **3.255 units/step** |
| 条件（A/B/C/D）による変動幅 | 最大 0.441 units/step |
| **比** | **penalty の効果は条件の効果の約 7.4 倍** |
| 転化確率 0.00 → 0.15 | 0.988 → 0.066（**15分の1**） |

**単一の未校正パラメータが、実験の主要因子より一桁大きく結果を動かしている。**
転化の成否は 0.00〜0.15 という狭い区間で完全に切り替わり、current model 0.35 は崩落点の外側にある。

### 8.4 penalty = 0.00 の位置づけ（固定）

> **`penalty = 0.00` は、modify による追加の成功確率ペナルティをゼロと仮定した
> モデル上の構造的上限ケースである。**
>
> **仕様適応による追加ペナルティをゼロと仮定する上限ケースでのみ、D4 の量的条件を超えた**
> （6.511 vs 6.0、約 +8.5%）。

**「penalty を下げれば転化する」「熟練を高めれば転化する」とは書かない。**
**`penalty = 0` が現実に達成可能であるとも、物理的に絶対不可能であるとも断定しない。**
現実の値は**未知**であり、その決定は実地 Pilot の測定課題である。

### 8.5 D4 供給閾値相当の感度境界 P\* — 適応コストがどこを超えると閾値に届かなくなるか

> **D4 供給閾値相当の感度境界（D4 share-equivalent sensitivity boundary）: P\* = 0.0426**

期待供給率が D4 の必要供給率 6.0 units/step とちょうど等しくなる penalty の値である。

**「損益分岐点」とは呼ばない。** 利益・損失の境界ではなく、
**D4 の量的条件を満たすかどうかが切り替わるパラメータ上の境界**だからである。

#### 導出（閉形式の近似）

供給経路の 95% は `n_shifts = 2`（2属性を各 +0.15）であり、`p_base` は 99.6% が上限 0.98 に
クリップされている（L12）。したがって供給率は penalty `P` に対しほぼ

```
C(P) = C(0) / (1 + 2P)          C(0) = 6.511 units/step
```

で減衰する。`C(P) = 6.0` と置いて解くと

```
1 + 2P = 6.511 / 6.0 = 1.085167
     P = (1.085167 − 1) / 2 = 0.042583
```

#### 厳密解（全 1110 make 試行の実分布による数値解）

`n_shifts` と `p_base` の実際の分布、およびクリップ `max(0.02, min(0.98, ·))` を含めて
二分法で解くと

```
P* = 0.042573      （そのときの期待供給率 = 6.000000 units/step）
```

閉形式の近似 0.042583 とほぼ一致する（差 1.0×10⁻⁵）。近似が妥当なのは、
`n_shifts=2` かつクリップ上限が支配的なためである。

#### 検算

| P | 期待供給率 (units/step) | D4 (6.0) |
|---|---|---|
| 0.000000 | 6.5109 | 超 |
| 0.037573 | 6.0558 | 超 |
| **0.042573 (= P\*)** | **6.0000** | **境界** |
| 0.047573 | 5.9452 | 未達 |
| 0.150000 | 5.0084 | 未達 |
| 0.350000 | 3.8299 | 未達 |

条件A のみで算出すると **P\* = 0.0370**（20 run 全体では 0.0426）。

#### 意味

| | 値 |
|---|---|
| D4 供給閾値相当の感度境界 | **P\* = 0.0426** |
| current model の値 | 0.35 |
| **比** | **現行値は感度境界 P\* の約 8.2 倍** |

> **適応コストが「1回の作り替えあたり実効難度 +4.3%」を超えると、
> 本モデルの量的条件には届かなくなる。**

**この数値が意味しないこと**（§11 と同じ制約が適用される）:

- ~~現実の適応コストが P\* より大きい / 小さい~~ — **現実の値は未知である。**
  P\* はモデル内の境界値であって、現実の測定値ではない
- ~~penalty を P\* 未満に下げれば転化する~~ — P\* は `modify_difficulty_penalty` を
  外生的に動かしたときの境界であり、現実に下げる手段があることを含意しない
- **P\* が示すのは「境界が現行値からどれだけ離れているか」だけである。**
  この 8.2 倍という距離こそが、§8.3 の感度の大きさを一つの数値で表したものであり、
  実データによる校正が必要な理由である

### 8.6 事前予測の検証

実行前に記録した予測が的中した。

| | 予測（実行前記録） | 実測 | 差 |
|---|---|---|---|
| 条件A、penalty=0.00 の供給率 | **6.45 units/step** | **6.443** | −0.1% |

---

## 9. Live-run Validation

§8 の replay は **Agent の意思決定をログに固定した partial-equilibrium** である（L13）。
この限界を限界のまま残さず、測定値に変えた。

### 9.1 設計（事前登録済み、実行前に固定）

条件A、seed 2 / 4、`modify_difficulty_penalty = 0.00`、他は main experiment と完全に同一。
**2 run で打ち止め。** 判定量 `D = Y − X`、許容帯は二項ノイズ `±2sd`。

### 9.2 結果

| seed | replay 予測 X | live 実測 Y | **D = Y − X** | 許容帯 | 判定 |
|---|---|---|---|---|---|
| 2 | 49.980 | **51.0** | **+1.020** | ±2.00 | **一致** |
| 4 | 54.880 | **55.0** | **+0.120** | ±2.10 | **一致** |

費用 $1.808630 / cap $3.00。両 run とも 48 calls、`status=completed`。

参考（本検証の目的ではない）: 両 run とも転化 TRUE（step 159、E4）。
share は seed2=0.2615、seed4=0.2764。

### 9.3 記録する結論

> **今回の範囲では、penalty 変更に対する Agent の behavioral response は二項ノイズを超える
> 大きさでは観測されず、partial-equilibrium replay は感度分析の近似として整合した。**

**n=2 のため、次のいずれも主張しない**：

- ~~一般的に replay が常に妥当である~~
- ~~behavioral response が存在しない~~
- ~~他の penalty 値でも同様である~~

**この2 run は条件間比較・仮説検証・主結果のいずれにも使用しない。**

---

## 10. What the Model Supports

以下は**モデル内で観測された事実**として固定する（人間確定 2026-08-16）。

| # | 内容 |
|---|---|
| **A** | **Main experiment**: 正式事前登録 D4 による **Transition = 0 / 20** |
| **B** | **中間機構**: 仕様適応・供給者形成・継続・協調が**モデル内で観測された**（各 20/20 run） |
| **C** | **Bottleneck decomposition**: 現行モデルでは、材料・設備・時間ではなく **modify 後の制作成功確率**が主要律速として現れた |
| **D** | **Sensitivity**: `modify_difficulty_penalty` への感度が**非常に大きい**（供給率 −50%、転化確率 15分の1、条件効果の約 7.4 倍） |
| **E** | **Live validation**: 2 seed で replay / live の差が事前登録許容帯内だった |

**加えて、方法論上の成果**：

- 転化は**物語ではなくコードで判定**した。LLM の文章を根拠にしていない
- **答えを与えずに**仕様適応が起きた経路を、make 試行ごとの provenance に直接記録した
- A/C・B/D のネットワーク同一性、pre-network 初期状態の4条件一致を検証した
- 事前登録と実行 config の同期不良（§5）を検出し、既存ログを改変せずに是正した

---

## 11. What the Model Does NOT Support

**以下はいずれも本研究が示したことではない。**

| # | 主張してはならないこと |
|---|---|
| 1 | ~~**現実のコスプレイヤーが危機時に供給できることを証明した**~~ — 本 PoC はモデル内実験であり、**実データでキャリブレーションされていない** |
| 2 | ~~**現実の供給量を予測できる**~~ — 本モデルの数値は現実のスケールを持たない |
| 3 | ~~**penalty = 0 なら成功する**~~ — 0.00 は構造的上限ケースの仮定であり、現実の値は未知 |
| 4 | ~~**Transition 0/20 は失敗である**~~ — 4条件中3条件は全 run で充足された。未充足は量の1条件のみ |
| 5 | ~~**閾値を下げれば成功した**~~ — 研究側の閾値は事前登録値のまま変更していない |
| 6 | ~~**材料備蓄・共用設備・時間確保は現実でも無効である**~~ — モデル内で律速でなかっただけであり、共有プールの競合・調達リードタイム・設備稼働待ちは本モデルに存在しない |
| 7 | ~~**コスプレコミュニティが他の趣味コミュニティより強い / コスプレイヤーが利他的である**~~ — 検証対象であってモデルの前提ではない（SPEC §6） |
| 8 | ~~**A/B/C/D の比較から現実のネットワークについて因果主張できる**~~ — structured topology は現実の再現ではなく検討用の仮説的構成（SPEC §19） |
| 9 | ~~**H1 / H2 / H3 が支持された / 否定された**~~ — n=5/セルは結論を出せる規模ではない。M1 の holdout では H1 は事前登録基準で**支持されなかった** |
| 10 | ~~**相互学習構造が供給能力を高める**~~ — peer learning 主効果は seed 間ばらつきに埋もれている。`coordination_edges` はむしろ逆方向だった |

---

## 12. Limitations

全 16 件は `docs/LIMITATIONS_CANDIDATES.md` にある。結果の読み方に直接効くものを挙げる。

### 12.1 モデルの構成に起因するもの

| # | 内容 |
|---|---|
| **L3** | **maker_count の天井効果**。全条件 30/30 で sd 0.00、156 step 中 30 step で飽和。Case C（中間機構は動くが下流への伝播が弱い） |
| **L12** | **技能の飽和**。蓄積相後の技能 0.9976〜0.9985、`p_base` の 99.6% が上限 0.98 にクリップ。**`success_probability` の技能項はショック相で情報を持っていない。** したがって「技能を上げる介入が効かない」は**現実の発見ではなくモデルの構成上の帰結** |
| **L14** | **D5 正規化により人数の効果が share から相殺されやすい**。`B = 3n` のため代数的には `share = q/(q+3)` で n が消えるが、これは `q` が n 非依存の場合に限る。production 層では n 非依存が構造的に成立する一方、decision 層（neighbor_proposals・agent 選出・誘導部分グラフ密度）では保証されない。**n を変えた実験は実施していない** |
| **L10** | D5 = `3 × shock_agent_count` により **share の上限が構造的に 0.5 に固定**される |

### 12.2 指標の識別力に関するもの

| # | 内容 |
|---|---|
| — | **Emergence level が transition の言い換え**になっている（E0/E1/E3 が 0 件） |
| — | **`steps_to_first_community_supply` が全 run で 0**（分散ゼロ、指標として機能していない） |
| — | **転化時刻の分解能不足**。8 step で `duration>=4` を要求するため実質 2 値 |
| — | **coordination が飽和**。全 20 run が閾値を達成し、条件を分離する指標として働いていない |
| — | **解の多様性が極端に低い**。供給に使われた制作対象は 2 種のみ |

### 12.3 推論範囲に関するもの

| # | 内容 |
|---|---|
| **L8** | **D5 に外部の実証的根拠が存在しない**。COVID 期のメイカー活動に対応する定量データがない |
| **L13** | **感度分析は partial-equilibrium**。「意思決定が同じままなら penalty がどれだけ供給量を動かすか」であり、「penalty が違う世界での供給量」ではない（§9 で 2 seed のみ検証） |
| **L15** | `community_supply_share` と parity utilization ratio は**別量**。混同してはならない |
| **L16** | `penalty = 0.00` は**構造的上限ケースの仮定**であり、現実に達成可能とも絶対不可能とも断定しない |
| — | **conditional sample**。9 seed 中 4 seed（44%）が構造的に測定不能で除外されている |
| — | **n=5/セル**。条件ごとの結果は seed 1本の入れ替わりで反転しうる |
| — | **感度分析グリッド 15 セル（L4）は未実施** |

### 12.4 プロセスに関するもの

| # | 内容 |
|---|---|
| **L7** | coordination_edges が原理的に到達不能だった実装欠陥（P0、修正済み） |
| **L9** | shock agent 選出の pairing 不整合（P0、修正済み。旧 scan は `INVALIDATED` として保存） |
| **L11** | **事前登録 D4 が runtime config へ同期されないまま実行された**（P0、§5。是正済み） |

---

# 製品への接続（ここから先は研究結果ではなく製品仮説）

> **§13–§14 は研究結果からの直接の含意ではない。**
> 本研究が特定したのは「商用化の前に現実で何を測らなければならないか」であって、
> 「何が売れるか」ではない。

---

## 13. Product Implications

### 13.1 研究が特定したのは「測るべき対象」である

本研究から「コスプレ制作能力が売れる」とは主張しない。研究が示したのは次の一点である。

> **供給能力の診断結果は、実データで校正されていない単一パラメータ
> `modify_difficulty_penalty` によって、「D4 到達」から「到達確率ほぼ 0」まで振れる。**

したがって、

> **校正なしに診断結果を自治体へ提示することはできない。**

### 13.2 最重要の測定項目 — Specification Adaptation Success

> **実際のコスプレ制作者が未知仕様へ適応するとき、成功率・制作時間・歩留まりが
> どの程度変化するか。**

これが `modify_difficulty_penalty` に相当する実データであり、**商用化前に測るべき第一の量**である。

### 13.3 現時点の商品仮説（これ以上に強めない）

> COSPLAY RESERVE は、**「コスプレ制作文化が未知仕様にどこまで適応でき、
> 量・成功率・時間・材料・設備のどこが供給を制約するかを診断する」**プロダクトである。

**「誰も作れない仕様の最初の数十個を供給するサービス」という強い商品定義は、
実地 Specification Adaptation Challenge で現実データを得るまで仮説として保持する。**

### 13.4 診断そのものの設計への含意

- **Simulator の価値は「診断値を出すこと」ではなく「その診断がどのパラメータに依存するかを
  明示できること」に置く。** §8 の感度の下では、単一の数値を返す診断は誤診になる
- **転化基準は顧客が設定できる必要がある。** 病院がフェイスシールド 50 枚を必要とする場合と
  マスク 5000 枚を必要とする場合では基準が異なり、50 枚の案件に 5000 枚の基準を当てれば
  埋められる穴を「埋められない」と誤診する。**研究側の閾値は事前登録値のまま変更しない**
- **モデル内で材料・設備・時間が律速でなかったことを、現実の資材配置設計の根拠にしてはならない**
  （§11 の 6）。本モデルには共有プールの競合も調達リードタイムも存在しない

### 13.5 原則

> **「文化を徴用する」のではない。「文化の中で育った能力に、新しい活躍先を作る」。**
> 「安全保障に参加してください」ではなく、**「好きなことを続けてください。それが安全保障になる社会」**
> （SPEC §31）。

個人の制作者は buyer ではない。**本人が希望した場合にのみ**能力を登録し、
案件提示に対して**任意で**参加する。報酬を伴ってよい。

---

## 14. Next Real-world Pilot

### 14.1 Specification Adaptation Challenge

**社会実装上の次の必須工程。** 感度分析の結果にかかわらず実施する。

実際のコスプレ制作者について、未知仕様の課題を提示し、以下を測定する：

| 測定項目 |
|---|
| 未知仕様への**適応成功率** |
| **modify 回数**（何回作り替えたか） |
| **modify による成功率の変化**（← `modify_difficulty_penalty` 相当量の校正） |
| 制作時間 |
| 材料 |
| 設備 |
| 成功 / 失敗 |

**目的は `modify_difficulty_penalty` 相当の値を実データで校正することである。**
これが得られるまで、本モデルの供給量診断は現実のスケールを持たない。

### 14.2 Pilot の体制（仮説）

三者を組ませる。**予算主体とチャネルは別である。**

| 関与者 | 持つもの | 持たないもの | 役割 |
|---|---|---|---|
| 自治体（防災・危機管理・文化振興・地域産業） | 予算、危機時の需要 | コミュニティへの接点 | 予算主体 |
| コスプレイベント運営会社 | 参加者との接点、登録の導線 | 予算、危機時の需要 | チャネル |
| コスプレ関連企業（造形材料・3Dプリンタ・ミシン・衣装） | 資材の供給網 | ボトルネックの所在 | 資材配置の設計主体 |

データ取得は**イベント運営会社の登録導線**で解く（参加登録に能力登録を1画面追加、任意）。

### 14.3 Pilot で検証されるべき仮説

| # | 仮説 | 対応する研究上の限界 |
|---|---|---|
| 1 | 未知仕様への適応時に成功率が下がる。その低下幅はいくらか | §7.3、L13 |
| 2 | 現実では材料・設備・時間のいずれかが律速になるか（本モデルでは律速でなかった） | §7.5、§11 の 6 |
| 3 | 能力登録スキーマが現実の制作能力を捉えられるか | 未検証 |
| 4 | 制作者が実際に参加を希望するか、どの条件で希望するか | 未検証 |

### 14.4 未着手の研究上の残件

- 感度分析グリッド 15 セル（L4）
- L12 の技能飽和への対処（蓄積相 step 数・`learn_rate` の見直し）。
  **ただし結果を見た後のパラメータ調整は禁止（SPEC §30）**であり、
  実施する場合は事前登録を伴う独立した実験として行う
- Emergence Level E0〜E4 の操作的定義の確定（暫定案のまま）
- Phase 2（未知ショックへの一般化）— Phase 1 の妥当性確認前には着手しない（SPEC §9）

---

## 付録: 再現に必要な情報

| 項目 | 場所 |
|---|---|
| 研究仕様（正典） | `SPEC.md` |
| 事前登録（H1 / D4 / D5 / seed） | `docs/PREREGISTRATION_H1.md` |
| 事前登録（感度分析 / live run） | `docs/PREREGISTRATION_SENSITIVITY.md` |
| 限界 L1–L16 | `docs/LIMITATIONS_CANDIDATES.md` |
| **主結果**（corrected adjudication） | `outputs/main_experiment/transition_recomputed_preregistered.json` |
| 副次的記録（実行時判定） | `outputs/main_experiment/{A,B,C,D}_seed{2,4,6,7,9}.json` |
| 実行時 config（歴史的記録、編集禁止） | `configs/as_executed/main_experiment_20260816.yaml` |
| 感度分析 | `outputs/sensitivity_replay/penalty_sensitivity.json` |
| live run | `outputs/live_penalty_zero/A_seed{2,4}_penalty0.json` |
| 事前登録監査 | `outputs/main_experiment/preregistration_audit.json` |

**現在の `configs/base.yaml` から実行すれば、正式事前登録 D4 による判定になる。**

| 実験 | LLM calls | 費用 |
|---|---|---|
| main experiment（20 run） | 960 | $18.503645 |
| live run（2 run） | 96 | $1.808630 |
| corrected adjudication / 感度分析 / 監査 | **0** | **$0.00** |
